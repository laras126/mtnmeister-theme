{% extends "page.twig" %}

{% block above_content %}
	
	<div class="contain">
		<ul class="row unstyle-list col-sm-2">
		
		{% for cat in cats %}
			<li>
				<a class="load" data-cpt="meister" data-id="{{cat.term_id}}"><strong class="cat-btn">{{cat.name}}</strong></a>
				{# <a class="load" data-cpt="gear" data-id="{{cat.term_id}}">load gear</a> // 
				<a class="load" data-cpt="deal" data-id="{{cat.term_id}}">load deals</a> #}
			</li>
		{% endfor %}

		</ul>
		<div class="col-sm-10" id="results"></div>
	</div>
{% endblock %}