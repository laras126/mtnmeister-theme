{% if post.categories %}

<ul class="list-cats meta">
    {% for cat in post.categories %}
        <li class="cat-item"><a href="{{cat.link}}">{{cat.name}}</a></li>
    {% endfor %}
</ul>

{% endif %}